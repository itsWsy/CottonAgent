/**
 * @typedef {'waiting'|'running'|'success'|'failed'} StepStatus
 * @typedef {'idle'|'running'|'success'|'failed'} TaskStatus
 * @typedef {'pending'|'accepted'|'rejected'} DecisionStatus
 */
export {}
